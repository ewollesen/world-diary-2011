- if permitted_to?(:edit, subject) && subject.veil_passes.present?
  %h2 Veil Passes
  %table{cellpadding: 0, cellspacing: 0}
    %thead
      %tr
        %th User
    %tbody
      = render(partial: "/shared/veil_pass", collection: subject.veil_passes)
